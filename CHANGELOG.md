# 更新记录

## 未发布

- 修复全新开发环境仅安装 `dev` 依赖时缺少 FastAPI、导致测试无法收集的问题。
- CI 现在直接验证 `dev` 依赖组能够独立运行完整测试，避免本机已有依赖掩盖配置遗漏。
- 持久化动作队列支持指数退避；重启后会继续遵守下次重试时间，避免故障设备触发紧密循环。
- 动作领取会写入持久化执行租约；工作进程崩溃后，其他进程等待租约到期再恢复任务。
- 队列通过跨平台操作系统文件锁串行化独立工作进程，避免同一动作被并发领取。
- JSONL 审计追加与归档使用跨进程文件锁，避免传感器、API 和队列并发写入时破坏记录。

## 0.4.0 - 2026-08-30

- 增加持久化动作队列，支持断电恢复、失败重试、处理数量限制和单设备故障隔离。
- 动作达到重试上限后进入死信区，可检查错误并使用新动作 ID 重新入队。
- 审计日志支持默认预览的原子归档，完整校验后拆分旧记录且拒绝覆盖已有归档。

## 0.3.0 - 2026-08-30

- 增加可校验的本地备份：生成 SHA-256 清单、检测篡改并安全恢复文件。
- 增加第 8 个教学项目和命令行入口，默认拒绝覆盖已有恢复目标。
- 本地中枢支持经过严格校验的 JSON 声明式规则，失败的热重载不会替换上一版有效规则。
- 备份轮换默认只生成预览，显式执行时也只清理校验通过的旧备份并保留可疑 ZIP。
- 运行状态控制台支持 Prometheus 文本指标，覆盖设备、告警、能源计划和最近动作。
- 健康快照持久化为本地样本，并汇总时间窗内的设备在线率和掉线次数。
- 增加只读审计查询器，支持类型、来源、闭合时间范围、条数过滤和统计摘要。
- 环境站支持自定义温湿度范围及相邻变化限制，并在采集后输出质量统计。
- 设备 API 支持持久化幂等请求键与写入审计，安全处理网络超时后的重复控制命令。
- 边缘视觉事件增加连续帧确认、按标签冷却去重、严格输入校验和本地处理统计。
- 能源调度为未安排负载给出最接近时段以及容量、时长缺口建议。
- 数字输出增加可测试的软件看门狗，命令超时后自动回到安全值并支持手动续期。
- 自动化中枢隔离单条规则的判断及动作异常，记录 `rule_error` 后继续执行健康规则。

## 0.2.1

- 修正 CI 报出的类型异常、导入顺序和无时区测试写法。
- 补上新版 Starlette 测试客户端需要的 `httpx2` 开发依赖。
- 升级 GitHub Actions，移除旧 Node.js 运行时警告。
- 修复设备 API 把鉴权依赖误当成查询参数的问题。
- 调整告警复发测试，使它与乱序心跳保护的实际行为一致。

## 0.2.0

- 环境站增加 DHT22 和 BME280 后端，读取失败会重试，CSV 可以接着上次的文件写。
- 增加本地运行状态控制台，把心跳、自动化动作、能源计划和告警放进同一份快照。
- 告警状态写入 JSONL 审计日志，支持确认、恢复后关闭和再次出现时重新打开。
- 能源调度会共享扣减时段容量和太阳能预算，并检查负载时长及输入范围。
- 局域网设备 API 增加写入令牌和退出清理；未配置令牌时只接受本机写入。
- 心跳和审计记录统一使用 UTC，旧心跳不会覆盖较新的设备状态。
- 漏水事件改为严格布尔值，字符串 `"false"` 不会再被当作漏水。
- README 按真实完成度重新整理，区分模拟演示、已有硬件适配和仍待真机验证的部分。

## 0.1.0

最初的 GPIO、局域网 API、边缘视觉、本地规则、设备心跳、能源调度和审计日志示例。
