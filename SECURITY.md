# 安全策略

## 支持范围

当前仅维护 `main` 分支和最新的 `0.5.x` 版本。早期教学版本不会单独回补安全修复；发现问题后
应先升级到最新提交，并重新运行部署预检、测试与依赖审计。

## 私密报告漏洞

请使用仓库 **Security → Advisories → Report a vulnerability** 私密报告，不要在公开 Issue 中
披露可利用细节、令牌、局域网地址、日志原文或真实设备信息。

报告应尽量包含受影响版本或提交、复现条件、预期与实际结果、影响范围，以及经过脱敏的最小
复现。维护者确认问题前，请不要扫描不属于你的设备或网络。

## 安全边界

本项目是树莓派本地自动化教学代码，不应直接暴露到公网。局域网监听必须使用长随机令牌，
硬件输出必须有物理隔离、保险和手动急停。软件测试和模拟通过不能替代真实负载的电气安全检查。
